Backup of the Bungee clone (BungeePage.tsx, bungee.css) and the original homepage (Landing.tsx). Restore Landing by routing '/' back to it in App.tsx.

CustomerAuth.tsx — the pre-redesign customer login/signup page, replaced by
market/AuthPage.tsx (which shares market/AuthShell.tsx with the professional
sign-in). Kept only for reference; it uses the old card styling, not the
current Bungee design system.
